<UserControl x:Class="View.Settings.Settings.StringSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:my="clr-namespace:View.Settings.Settings"
             >
	<my:SettingTemplate>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition/>
				<RowDefinition/>
			</Grid.RowDefinitions>

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<Label Content="{Binding Name}" Grid.Row="0" />
			<TextBox Text="{Binding Value, Mode=TwoWay}" Grid.Row="1" Margin="5,0,0,0"/>
		</Grid>
	</my:SettingTemplate>
	
</UserControl>
