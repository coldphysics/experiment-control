<UserControl x:Class="View.AdWin.Debug.FifoDebugView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       >
	<UserControl.Resources>
        <DataTemplate x:Key="BarsTemplate">
			<Grid HorizontalAlignment="Stretch">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="50"/>
					<ColumnDefinition Width="300"/>
					<ColumnDefinition Width="auto"/>
					<ColumnDefinition Width="auto"/>
					<ColumnDefinition Width="auto"/>
				</Grid.ColumnDefinitions>
				
				<Label Content="{Binding Path=ChannelName}" Grid.Column="0"/>
				<ProgressBar Value="{Binding Path=CurrentFifoLoadPercent}"  Grid.Column="1" Height="20" HorizontalAlignment="Stretch"></ProgressBar>
				<Label Content="{Binding Path=CurrentFifoLoad}" Grid.Column="2"/>
				<Label Content="/" Grid.Column="3"/>
				<Label Content="{Binding Path=TotalFifoSize}" Grid.Column="4" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

	<Grid HorizontalAlignment="Stretch">
		<ListBox IsSynchronizedWithCurrentItem="True" ItemsSource="{Binding Path=Bars}" ItemTemplate="{StaticResource BarsTemplate}" Name="BarsListBox" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

        </ListBox>
    </Grid>
</UserControl>
